# Gemini (Vertex AI) — required when provider is "gemini" (default)
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# OpenAI — required when provider is "openai"
OPENAI_API_KEY=sk-...
